{
  "name": "glish",
  "version": "1.0.0",
  "description": "Goal: Make a version of English where every word is only one syllable",
  "main": "main.ts",
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "buildSyllables": "ts-node syllablize.ts",
    "buildWords": "ts-node main.ts",
    "rebuildAll": "ts-node syllablize.ts; ts-node main.ts"
  },
  "devDependencies": {
    "@types/node": "^18.6.4"
  }
}
